## Packages
framer-motion | Page transitions and sleek entry animations

## Notes
- Application is strictly dark theme matching the provided design (#0d1117 background).
- Arabic typography uses Google Font 'Tajawal' for a premium, readable look.
- Layout uses `dir="rtl"` to correctly render Arabic text.
- Fallback placeholder used for the logo using `lucide-react`.
